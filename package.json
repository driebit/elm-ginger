{
    "scripts": {
        "test": "elm-format --validate src && elm-test"
    },
    "dependencies": {
        "elm": "^0.19.1-5",
        "elm-format": "^0.8.1",
        "elm-test": "^0.19.1-revision11"
    }
}
